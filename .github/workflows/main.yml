name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '12.22.9'  # Use Node.js version 12.x
        # You can specify a more specific version if needed, e.g., '12.22.9'

    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: npm test
